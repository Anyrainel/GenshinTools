# Checkpoint 50: Xiao FFXX guarded Circlet and local-substat diagnostic

This checkpoint executes the first missing-stat variants over the authenticated
Xiao FFXX weapon domain. It admits source-listed Circlet and offensive-tail
stat keys only as Guide Factory experimental factors, then measures local
sensitivity under one incomplete wrapper fixture. It does not select a Circlet,
allocate a legal artifact build, or produce a stat priority.

## Exact authenticated boundary

The durable report authenticates 127 exact raw-byte inputs: checkpoint 49's
complete 124-path boundary, the durable checkpoint-49 report, and the
checkpoint-50 core and CLI. Fifteen JSON inputs require both raw-byte and
parsed-object parity. Checkpoint 49 is fresh-authenticated before use, which
recursively rebuilds the complete Xiao source, candidate, replay, normalization,
and source-group diagnostic chain.

The report declares the same inherited 80-path replay-runtime set and zero
additions. This is a declared boundary, not a self-authenticated graph claim.
The focused AST test independently verifies that every declared runtime path's
first-party imports stay inside the set, every declared path is reachable from
the replay roots, and every direct checkpoint-50 core/CLI import is inside the
authenticated 127-path boundary.

Checkpoint 50 reconstructs the private checkpoint-47 wrapper fixture from the
fresh-authenticated checkpoint-46 candidates and checkpoint-42 calculator-
default team assumptions plus formula counts. Six no-Circlet control replays
must reproduce checkpoint 48's six technical totals and exact Xianyun
activation trace before any new variant is accepted.

## Source evidence versus experimental admission

Checkpoint 45 already authenticates the exact source rows needed here:

- the guarded CRIT Rate / CRIT DMG Circlet choice and its at-least-70% CRIT
  Rate condition;
- the guarded priority-1 CRIT Rate / CRIT DMG offensive tail and its near-1:2
  target; and
- the guarded priority-2 ATK% offensive tail.

Those rows remain `guarded-unresolved`. Checkpoint 50 does not claim that their
conditions are satisfied or that they form a complete substat plan. The source
authored the available keys and prose; the Guide Factory authored their use as
experimental factors, every numeric artifact value, the fixture, and every
comparison.

Technical node hashes retain checkpoint 46's candidate identity, including its
inherited present/missing-axis policy. They exclude checkpoint 50's guard-
resolution and publication-claim fields, the separate candidate-provenance
hash, and source rank. Source-group membership remains bound in authentication
and provenance and constructs only the twelve baseline validation pairs; it
does not alter a lattice total or technical node identity.

## Exact technical lattice

Six authenticated weapon origins each receive two level-20 five-star Circlet
variants, CRIT Rate and CRIT DMG, producing twelve no-substat baselines. Each
baseline receives one independent average five-star roll probe in CRIT Rate,
CRIT DMG, and ATK%, producing thirty-six local children. These forty-eight
lattice nodes are not a legal five-piece allocation and have no total-roll
budget or filler stats.

The operation ledger therefore separates six fixture-reconstruction controls,
twelve Circlet baselines, and thirty-six one-roll probes: fifty-four fresh
unit-expanded replays in all. Every replay must pass interpreted/compiled
agreement and preserve Xianyun's exact eight-active/three-inactive plunge trace.

## Diagnostic comparisons

The report retains three narrow diagnostic families:

- thirty-six parent-to-one-roll local deltas, without scalar weights or a
  global priority;
- six same-weapon CRIT Rate versus CRIT DMG Circlet deltas, without a winner or
  choice; and
- twelve cross-source-group pairs stratified by the same Circlet and zero
  substats, without mixed-Circlet, perturbed, within-group, or cross-rarity
  comparison.

Checkpoint 49's four exact counterexamples remain immutable upstream holdouts;
reducing them is not an optimization objective. Deathmatch participates in its
own baselines and local probes but in no cross-rarity pair.

## Unresolved source guard

The wrapper may report its exact CRIT Rate / CRIT DMG frame and distance from a
1:2 ratio, but the source guard remains unresolved. The source does not define
whether 70% or near-1:2 refers to panel, pre-Marechaussee, or max-stack combat
stats, and it supplies no tolerance for near. A threshold observation under
this fixture is not a Circlet decision or minimum-roll requirement.

## Capability boundary and next question

The diagnostic runs no generator, constrained-greedy allocator, owned-inventory
optimizer, AutoTune, ideal-roll allocation, or ER module. It supports no guide,
complete build, stat priority, weapon rank, winner, selection, damage claim,
rotation, gameplay, or DPS conclusion.

Before a later checkpoint may select or allocate, the user must define the
70%/1:2 reference frame and near tolerance, the legal artifact-quality and
total-roll policy, and whether non-listed non-ER filler stats may be optimizer
variables. ER remains deferred.

## Durable output and verification

The checked-in report is 266,139 bytes with SHA-256
`e562a388eb22ff78a469d4f6be26491e37a48bfdd04760df207cd3bb84341c03`.
The CLI authenticates the fresh in-memory report before writing it, and the
integrated validator authenticates the persisted JSON. The focused
checkpoint-50 suite passes 13/13 tests. The whole Guide Factory suite passes
97 files and 936 tests with 2 existing skips. Deferred-ER validation finishes
with 0 errors and 12 existing legacy weapon-type warnings. Guide Factory and
application TypeScript pass, and the application dependency boundary reports
no violations across 758 modules and 3,994 dependencies. The independent
trust-boundary and claim audit passed after the runtime, identity, source-group,
and fixture-origin claims were narrowed to the evidence actually authenticated.
The repository-wide Biome command remains red on the pre-existing formatter drift at
`.dependency-cruiser.cjs:280`; the Guide Factory subtree is excluded by the
current Biome configuration.
