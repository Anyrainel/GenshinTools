# Checkpoint 35: isolated Keqing evidence triangulation

This checkpoint completes one narrow, non-ER validation slice across three
unlike evidence types for Keqing: ArtifactRatingDB source-native heuristic
coefficients, attributed KQM equipment/stat observations, and the existing
four-endpoint local stat-marginal diagnostic. It keeps the comparison
sign-only and does not average the evidence into a weight, priority, rank, or
recommendation.

## Five-file authentication boundary

The standalone slice authenticates exactly five inputs:

- the isolated ArtifactRatingDB Keqing snapshot;
- the consolidated knowledge repository containing the attributed KQM record;
- the existing Keqing/Ineffa full-team local-marginal report;
- the raw KQM Keqing manual snapshot; and
- the existing Keqing Lunar equipment-evidence report.

The raw KQM source record and its consolidated recommendation must remain in
exact parity. The equipment-evidence input must independently retain its exact
Keqing/Ineffa/Furina/Xilonen team projection, default main-stat coverage, and
eight-claim projection. Seven claims are exact-team matched; the secondary
CRIT-overcap condition remains unresolved. These checks close the records used
by the wrapper; they do not make the wrapper-owned cross-record join source-
authored.

## Sign-only observation rows

The durable report contains exactly ten wrapper-enumerated, explicitly
unranked rows:

- four ArtifactRatingDB-nonzero rows whose Keqing local marginal is positive at
  all four endpoints;
- four ArtifactRatingDB-zero rows whose local marginal is zero at all four
  endpoints;
- one Elemental Mastery objective-coverage gap, where the source coefficient is
  positive but the pinned reaction-free local objective is zero; and
- one Electro DMG Bonus source-main-stat row outside the nine-stat local-
  marginal perturbation domain.

Only local sign, zero classification, and endpoint identities are projected.
ArtifactRatingDB coefficient magnitudes are not compared with local marginal
magnitudes. KQM priority values are neither sorted nor treated as a validated
order. ArtifactRatingDB leaves team, role, weapon, constellation, and scenario
unknown, so cross-source context comparability is not established.

The local diagnostic remains an unreviewed, reaction-free technical objective
with eight readiness blockers. The Elemental Mastery row therefore records an
objective-coverage mismatch rather than a source disagreement.

## Deferred energy and disabled capabilities

Both `SPRatioBase` occurrences remain present as deferred source evidence. No
ER input is consumed and no ER requirement is computed.

Guide production, ranking, scalar stat-weight production, promotion, and ER
production all remain false. The slice also performs no candidate generation,
optimization, ideal-stat allocation, or equipment/build recommendation.

## Permission and integration boundary

ArtifactRatingDB remains `planned`, with mixed permission, isolated-pilot-only
integration, and consolidation blocked pending permission review. This
checkpoint therefore adds no ArtifactRatingDB entry to the shared source
registry, consolidated repository, condition-binding catalog, or global
validator. It adds no application or Worker integration and cannot enter the
distributed bundle.

The standalone output is the thirty-second durable Guide Factory report. The
global validator continues to own only its already integrated report set; the
new report is authenticated through its isolated builder, CLI, and tests.

## Verification

The completed checkpoint passes Guide Factory TypeScript, 63 test files with
487 tests, application TypeScript, and dependency-boundary validation.

## Next non-ER checkpoint

The next bounded experiment is a generic, source-backed four-character
equipment candidate lattice. It is not implemented. It must preserve source
scope and alternatives without turning this sign-only observation into a
weight, ordering, recommendation, or cross-product. ER remains deferred.
