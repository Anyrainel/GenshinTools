# Checkpoint 53: Noelle Hexerei partial equipment composition

This checkpoint composes checkpoint 51's two authenticated Noelle artifact
profiles with checkpoint 52's one exact-team Gest applicability binding. It
produces two request-parameterized partial validation candidates, one for each
source investment branch. It does not choose which branch applies, select any
equipment or stats, or produce a complete build.

## Exact authenticated boundary

The durable report authenticates the exact same sorted 29-path text boundary in
both `sourceFiles` and `generatedFrom`. Every declared lowercase SHA-256 must
match the corresponding text. The closure is the union of:

- checkpoint 51's complete 23-path input boundary;
- checkpoint 52's complete 16-path input boundary, adding its core and CLI to
  the paths already present through checkpoint 51's high-slice inputs;
- the durable checkpoint-51 and checkpoint-52 reports; and
- the checkpoint-53 core and CLI.

Eight JSON inputs require authenticated-text and separately supplied parsed-
object parity: the consolidated repository, Noelle manual snapshot, manual
index, source registry, high- and lower-investment source-local reports, the
checkpoint-51 artifact-profile report, and the checkpoint-52 weapon/team-
binding report.

Checkpoint 53 reconstructs the complete checkpoint-51 and checkpoint-52 inputs
only from the outer authenticated path map. Each reconstructed input must match
the separately supplied upstream input exactly. The shared repository,
snapshot, index, registry, and high-slice objects must agree across every nested
upstream projection; the lower slice remains checkpoint-51-specific. This
prevents an upstream input object from diverging from the bytes authenticated by
checkpoint 53.

Both durable upstream reports are then fresh-authenticated from those
reconstructed inputs. Checkpoint 53 additionally requires their validation-
only capability and zero-operation boundaries to remain narrow before any
composition is admitted. The complete checkpoint-52 binding authorship and
assignment ledger is also pinned by exact semantic projection and SHA-256
before it can be cloned. A newly authenticated upstream implementation cannot
silently broaden checkpoint 53's authority.

## Two request-parameterized partial candidates

Checkpoint 51 supplies two artifact profiles in deterministic source order:

- Noelle C0-C5 with Burst Talent Level 9; and
- Noelle C6 or Burst Talent Level 10 or higher.

Checkpoint 52 supplies one exact Noelle — Durin — Nicole — Xilonen team and one
Gest of the Mighty Wolf applicability binding for that team. The consolidated
team member's Noelle investment remains `unspecified`. Therefore neither
investment predicate is evaluated, neither branch is selected, and the two
outputs are parameterized validation candidates rather than competing builds.

Each candidate retains the exact four-character roster, its complete cloned
checkpoint-51 artifact profile, the checkpoint-52 source observation and
binding, explicit null selections, completeness flags, authorship ledger, and a
deterministic candidate hash. The source authored neither candidate nor the
combined profile/weapon/team composite. The Guide Factory authored only the
deterministic partial projection.

## Projected weapon and artifact-set options

Gest of the Mighty Wolf is projected once into each candidate: one unique
weapon and two projected occurrences. It remains an applicable but unranked
option. Refinement and quantitative performance remain `missing-not-zero`, and
`selectedWeapon` remains null in both candidates.

The embedded checkpoint-51 profile still says that its own weapon boundary is
`missing-not-zero`. Checkpoint 53 does not rewrite that upstream field; it
projects the authenticated Gest option separately beside the unchanged profile.

Four-piece Husk of Opulent Dreams is likewise retained once per candidate: one
unique artifact set and two projected occurrences. It remains an unassigned
source option, while `selectedArtifactSet` remains null. No legal artifact
inventory or complete artifact assignment is constructed.

## Exact artifact-profile preservation

The two embedded artifact profiles are exact clones of the fresh-authenticated
checkpoint-51 profiles. Across them, checkpoint 53 retains:

- six main-stat slot groups, covering Sands, Goblet, and Circlet for each
  investment branch;
- eight main-stat option groups: three lower-investment options and five high-
  investment options;
- all six ordinal offensive substat groups and their exact occurrence,
  condition-hash, and new source-binding provenance; and
- every guarded alternative and source condition unchanged, including the
  high-investment DEF% Goblet and DEF% Circlet alternatives.

Every slot's selected option index remains null. The candidate-level Sands,
Goblet, and Circlet selections are also null. The ordinal substat groups remain
non-scalar source priorities, with no selected allocation and no scalar weights.

## Candidate construction versus runtime generation

Checkpoint 53 records `candidateGenerationExecuted: true` only with the exact
kind `deterministic-authenticated-branch-projection`. It performs one projection
run that constructs two partial validation candidates from the authenticated
two-by-one upstream domain. The narrowly named
`supportsPartialValidationComposition` capability is the only positive
capability.

This is not the existing runtime candidate generator. Each candidate records
`existingRuntimeGeneratorProducedCandidate: false`, while
`generatorExecuted` remains false and `generatorRunCount` remains zero. The
authorship ledger also records `sourceAuthoredCandidate: false`,
`sourceAuthoredComposite: false`, and Guide Factory ownership of the partial
composition and projection.

## Preserved computation and missing-evidence gates

Checkpoint 51's complete computation-representation admission object is copied
unchanged into the report. Its status remains
`rejected-exact-formula-representation-missing`: the source rotation's `N3D`
and `N2` prefixes cannot be represented by the inseparable four-hit
`noelle-na` entry. No numeric replay is admitted and its result remains null.

Checkpoint 52's missing refinement and quantitative-performance boundaries are
also preserved in both Gest projections. Applicability to the exact source
section is not sufficient evidence for a refinement, weapon comparison, rank,
or selection.

## Capability and operation boundary

Beyond the one deterministic partial-candidate projection, checkpoint 53 runs
no recommendation composition, existing runtime generator, optimizer,
AutoTune, team or complete-build composition, weapon, artifact, or equipment
assignment, selection, ranking, damage computation, rotation replay,
ideal-stat allocation, or Energy Recharge computation. It emits zero complete
builds, assignments, selections, numeric results, and optimizer outputs.

It supports no source authorization, guide, team recommendation, build
recommendation, equipment recommendation, stat recommendation, rank, damage,
rotation, ideal-allocation, or ER claim. In particular:

- do not choose an investment branch from the two projections;
- do not treat Gest, Husk, a main stat, or a substat allocation as selected;
- do not rank the candidates, weapons, artifact options, or stat groups;
- do not treat the candidates as source-authored teams or builds; and
- do not infer damage, rotation feasibility, DPS, ideal rolls, or an ER target.

## Verification and durable output

The focused checkpoint-53 suite passes 16/16 tests. It covers deterministic
self-authentication, the 29-path and eight-JSON boundary, every upstream object
projection, fresh CP51 and CP52 authentication, candidate/profile/provenance
preservation, operation and capability boundaries, static local-import closure,
and fail-closed raw, hash, JSON, upstream-authority, output, and serialized-
report tampering.

Guide Factory TypeScript and the no-write workspace builder pass. The builder
reconstructs and authenticates the report without writing the durable output.

The durable report is
`scripts/guide-factory/reports/noelle-hexerei-partial-equipment-composition.json`.
It is 34,701 bytes with SHA-256
`1cca0c987f0590aff79c7d1e215d298ec47fe139429d8958c295c08eadf7e99c`.

## Next step, not a complete factory

Checkpoint 53 is an authenticated partial composition, not a working guide
factory. A next non-ER checkpoint should close one of two concrete boundaries:

- add an offline computation representation that can express the source
  rotation's exact Normal Attack prefixes before attempting a replay; or
- authenticate the missing equipment and fixture evidence needed for a bounded
  comparison, including refinement, quantitative weapon behavior, enemy
  scenario, complete artifact assumptions, and the comparison domain.

Either path must preserve the current null selections, request-parameterized
investment branches, source-versus-Guide-Factory authorship, and validation-
only authority until its own evidence gate passes. ER remains deferred.
