# Checkpoint 34: authenticated lower-investment Noelle catalog admission

This checkpoint separately admits the three already authenticated checkpoint 33
lower-investment Noelle main-stat occurrences to the current condition-binding
catalog. It adds no source record or durable report. Admission requires the
checked-in lower-investment report to equal a fresh source-specific rebuild and
repeats the complete Noelle-specific boundary before any shared normalized
entry-construction helper may run.

## Separate admission gate

The admitted source-local slice remains
`kqm-noelle-source-local-lower-investment-slice-luna-viii`. Its exact guide
record is
`kqm:character-guide:noelle-c0-c5-talent-9-artifact-stats-luna-viii`, and its
wrapper-owned validation team remains
`kqm:team:noelle-durin-nicole-xilonen-hexerei-example-luna-viii` with the
ordered roster Noelle, Durin, Nicole, and Xilonen.

The catalog gate reauthenticates the exact page/version and snapshot, raw-input
and generated-from closure, source document and team controls, numeric request
context, source predicate, request predicate, selected payloads, source claims,
condition controls, recommendation metadata, occurrence identities, and
disabled capability boundaries. Only after those source-specific checks pass
may the private normalized helper cross-link selected occurrence, claim, and
control and construct catalog entries. The helper is not a generic source
wrapper, schema adapter, extractor, or prose parser.

## Exact three-occurrence admission

The catalog admits only:

- ATK% Sands;
- Geo DMG Bonus Goblet; and
- CRIT Rate/CRIT DMG Circlet.

All three retain the exact source condition `Noelle is C0–C5 and her Burst
Talent is Level 9.` and the wrapper-owned request predicate
`all(constellation-at-most(Noelle, 5), talent-level-is(Noelle, burst, 9))`.
Independent C5 and Burst Talent 9 request facts remain scoped to the exact team
and Noelle subject. The source cells remain unresolved while the supplied
request context makes the three effective cells matched. Catalog admission
records that authenticated mapping; it does not turn a typed binding into proof
that the condition is true for another team or account.

The lower- and high-investment Noelle branches remain occurrence-disjoint.
Their Geo DMG Bonus and CRIT payload hashes, and their unresolved source
predicate hash, can coincide without authorizing an alias: the source record,
occurrence path, condition hash, slice ID, selected object, and request
predicate remain distinct.

## Holdout closure

The authenticated Noelle boundary remains exactly 16 arrays: three selected
main-stat occurrences, 12 named nonempty holdouts, and one empty occurrence.
All 12 holdouts, including every substat row, and the empty occurrence remain
outside the catalog. They are unconsumed and receive no new binding or energy
classification. Admission neither broadens the slice nor infers a rule from
matching prose or payload hashes.

## Catalog and manual-coverage results

The authenticated current catalog is now 63 entries: 60 typed bindings and 3
exact-text acknowledgements. Its independent energy classifications are 57 not
energy-deferred, 3 deferred-energy prerequisites, and 3 energy-unclassified
acknowledgements. The three new typed entries are explicitly not energy-
deferred; no ER requirement is computed.

Across the 126 nonempty manual condition arrays, full binding coverage is:

- 60 typed occurrences carrying 82 condition-string occurrences;
- 3 acknowledged occurrences carrying 3 condition-string occurrences; and
- 63 unbound occurrences carrying 74 condition-string occurrences.

Excluding only the three structural ER arrays leaves 123 non-structural rows:
60 typed with 82 strings, 3 acknowledged with 3 strings, and 60 unbound with 71
strings. The 86 unique non-structural arrays split into 34 typed-only, 51
unbound-only, and one mixed acknowledged/unbound set.

The independent energy ledger is 15 deferred, 57 explicitly not
energy-deferred, 54 nonempty unclassified, and 16 empty unconditional arrays.
The deferred total remains three structural ER rows, three typed Itto
prerequisites, and nine exact authored Diona/Furina rows. Display status is 57
typed, 51 known-but-unbound, 15 ER-deferred, 3 acknowledged, and 16
unconditional occurrences.

Manual coverage now authenticates eight wrapper families through 19 raw source
files and 73 generated-from paths. The report corpus remains 31 durable reports.

## Downstream witness boundary

The Klee claim-join witness is regenerated only because manual coverage is an
authenticated upstream input. Its raw-input boundary remains six source files,
its generated-from boundary becomes 76 paths, and its authenticated upstream
binding count becomes 63.

Its semantics do not change: the positive Klee/Furina/Albedo/Xilonen fixture
still carries four independent claims from two source records; the exact
Klee/Chevreuse/Durin/Fischl control still retains three applicable role claims
and one source-definitely-inapplicable Furina condition; and all 11 Klee
holdouts remain unconsumed. The witness still creates no build or candidate.

## Verification

The completed checkpoint passes Guide Factory TypeScript, 62 test files with
464 tests, and validation with 0 errors and 12 existing warnings.

## What this checkpoint does not establish

Checkpoint 34 creates no team, build, equipment assignment, candidate,
recommendation, rank, optimizer result, formula plan, damage result, rotation,
stat weight, ideal-roll allocation, or ER requirement. It does not authorize or
human-review the source, establish payload optimality, modify application or
Worker code, enter a published bundle, or create a player-facing guide.

## Next non-ER checkpoint

The next bounded experiment is an isolated Keqing triangulation across the
ArtifactRatingDB source-native heuristic coefficients, attributed KQM build
evidence, and the existing full-team local-marginal diagnostic. It is not
complete and must compare unlike evidence without averaging it into a weight or
recommendation.

ArtifactRatingDB remains outside the shared source registry and consolidation
pipeline. Its permission posture stays mixed and consolidation stays blocked
pending review; the triangulation must not redistribute or promote that pilot
as active knowledge. No checkpoint 35 implementation is claimed. ER remains
deferred.
