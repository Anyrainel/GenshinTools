# Checkpoint 43: authenticated Xiao FFXX applicable-claim projection

This checkpoint consumes the authenticated checkpoint-42 Xiao condition slice
through a new source-specific projection. It answers one deliberately narrow
question: which of the three already-authenticated occurrences are applicable
for the exact FFXX team with source facts alone, and which become applicable
when the exact wrapper-owned Xiao-C6 request fact is supplied?

This is still not a working guide factory. It does not select equipment,
compose a build, enumerate or rank candidates, evaluate compatibility, run the
generator or optimizer, evaluate formulas or damage, optimize a rotation,
allocate ideal rolls, or calculate Energy Recharge.

## Two authenticated views

The source-only view retains the exact Xiao/Xianyun/Furina/Faruzan team and
contains:

- two source-matched occurrences: Marechaussee Hunter under Xianyun and Anemo
  DMG Bonus Goblet under Xianyun;
- one unresolved occurrence: the separate Xiao-C6 Anemo Goblet condition;
- two canonical payload identities; and
- no request facts.

The exact-request view adds only one Guide Factory-owned fact: Xiao is C6 for
that exact team and character scope. It contains:

- the same two source-already-matched occurrences;
- exactly one request-resolved occurrence, the C6 Anemo Goblet condition;
- all three occurrences matched; and
- the same two canonical payload identities.

The request fact is not attributed to KQM and cannot escape the exact FFXX and
Xiao scope. The source team itself still leaves Xiao's constellation
unspecified.

## Payload identity without voting

The two Anemo Goblet occurrences have the same payload. The projection groups
them by the exact technical identity tuple `claimAxis`, `mainStatSlot`, and
`payloadSha256`, while retaining both occurrence IDs and their different
applicability provenance.

This grouping is deduplication only. Multiplicity does not establish
corroboration, confidence, preference, ranking, or a second vote. The artifact
set and Goblet axes are not composed or tested for compatibility.

## Authentication boundary

The projection byte-authenticates all seventeen exact declared input files.
Five JSON inputs also require parsed-object parity, including the durable Xiao
source-local report, which is fresh-authenticated from its own upstream inputs.
The projection pins the exact 3 selected / 14 holdout / 4 empty partition. The
three partitions are pairwise disjoint; no holdout or empty occurrence is
consumed.

Every projected occurrence embeds and hashes its selected occurrence, source
claim, condition control, source cell, and request projection. The exact sole
request context is one Xiao-C6 fact for the exact FFXX team. Any raw-input,
declared-hash, durable-report, semantic-lineage, request-scope, payload, view,
partition, or capability drift makes the report non-comparable.

The durable report is
`xiao-ffxx-applicable-claim-projection-contract.json`. It is 41,394 bytes with
SHA-256
`92a6cd2eda00891477189387686e57142c8ab10d540edeff4d795b0ee58e07d0`.

## Broader-source boundary

A read-only Crimson Witch probe identified a potentially useful pre-C6 Faruzan
artifact branch that would broaden the current C6-focused baseline. The site
has no documented reusable recommendation feed or reuse grant, so no payload,
snapshot, semantic hash, or recommendation content was retained. Crimson Witch
remains blocked and permission-required in the source registry. Its existing
source profile describes the adapter boundary that may be used only after
permission or a documented reusable feed exists.

## Verification and next boundary

The checkpoint is integrated into the offline validator as the fortieth
globally rebuilt report. Including checkpoint 35's permission-isolated report,
the reports directory contains 41 JSON reports. The validator fails closed on
non-comparable inputs or a saved report that differs from a fresh rebuild.

The completed checkpoint passes:

- Guide Factory TypeScript;
- 90 Guide Factory test files with 856 passed tests and 2 skipped tests;
- offline validation with 0 errors and the 12 existing corpus weapon-type
  warnings while historical Diona/ER regeneration is deferred;
- application TypeScript; and
- dependency-boundary validation.

The next non-ER step is a bounded, explicitly Guide Factory-authored partial
candidate seam. It may compose only the two applicable artifact axes from this
authenticated projection, must preserve all missing build axes as missing, and
must retain both source-only and exact-request applicability views. It must not
silently promote the partial candidate into a recommendation or complete Xiao
build.
