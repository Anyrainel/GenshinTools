# Checkpoint 48: authenticated Xiao FFXX unit-expanded execution gate

This checkpoint accepts only the representation-normalized part of checkpoint
47's evidence. It turns six independently verified unit-expanded observations
into inputs eligible for a later same-fixture technical comparison. It does not
perform that comparison and does not produce a weapon ranking or guide claim.

## Exact authenticated boundary

The durable report authenticates 121 exact raw-byte inputs: checkpoint 47's
complete 118-path boundary, the durable checkpoint-47 report, and the
checkpoint-48 core and CLI. Thirteen JSON inputs require both raw-byte and
parsed-object parity.

Checkpoint 47 is fresh-authenticated before use, recursively rerunning its six
grouped replay attempts, six grouped dual-path raw captures, six unit-expanded
agreements, and one rejected Flower/Plume probe. Checkpoint 48 does not copy or
restate an unauthenticated fixture and makes zero additional calculator replay
calls.

## Fixture-specific representation normalization

The normalizer is deliberately scoped to this Xiao checkpoint. It accepts only
positive safe-integer counts whose sum does not exceed the exact thirteen-line
fixture bound. It expands each counted line contiguously in wrapper-authored
order while preserving character, formula, reaction, and on-field fields.

The independently normalized plan must be exactly equal to checkpoint 47's
freshly authenticated unit-expanded plan. This establishes one safe execution
representation for this fixture; it does not claim that arbitrary counted,
fractional, ranged, optional, or timing-sensitive plans may be expanded this
way.

## Six technical observations

Each candidate is eligible only when all of these exact invariants hold:

- grouped interpreted damage equals unit-expanded interpreted damage;
- unit-expanded interpreted and compiled totals agree;
- Xianyun's eleven plunge-unit activations are exactly eight active followed
  by three inactive; and
- the candidate remains in the authenticated condition-free source-only C0
  domain, excluding the separate wrapper-C6 request view.

Both numeric differences are recomputed locally from the three authenticated
totals instead of trusting upstream agreement flags or stored deltas. The
normalization-policy hash includes the count rules, ownership, ordering,
field-preservation semantics, exact counts, and mappings. All six observations
pass and are serialized by technical candidate ID. This ordering is not a
damage order or a source rank.

## Provenance and capability boundary

The two ranked five-star source groups and the unranked Deathmatch group are
retained as three provenance-only groups with six membership edges. Their
rank, rarity, order, and tied membership do not affect the execution-gate
identity.

The report records `comparisonExecutionStatus: not-performed`. Pairwise
comparison count, rank count, winner count, and selection count are all zero.
It therefore supplies no damage comparison, weapon recommendation, guide,
complete build, stat priority, ideal-roll allocation, rotation, gameplay,
generator, optimizer, AutoTune, or ER claim.

## Next non-ER boundary

The next experiment should compare the six eligible observations with the two
ranked five-star source groups under this exact incomplete wrapper fixture.
Deathmatch must remain cross-rarity-incomparable. Group overlap and every
counterexample should be emitted only as validation targets: source order is
not ground truth, and computed damage under this partial fixture is not a
correction to the source.

Only after that discrepancy report is credible should the experiment add
guarded Circlet or substat variants. ER remains deferred.

## Durable output and verification

The checked-in report is 40,834 bytes with SHA-256
`1932888712ff16a7008167f540c4d284029fe828c3cd129e096a4cfda32309d9`.

Focused checkpoint tests pass 10/10. The complete Guide Factory suite passes
95 files with 912 tests passed and two historical tests skipped. The global
validator finishes with zero errors and twelve pre-existing catalog warnings
when Diona comparison and ER calibration are explicitly deferred. Guide
Factory TypeScript, the application-wide type check, and dependency-boundary
validation also pass.
