# Checkpoint 56: Noelle Hexerei local stat-priority diagnostic

## Outcome

Checkpoint 56 adds the first source-conditioned, one-roll stat-response lattice
to the Noelle path. It does not emit a stat priority, scalar weight, selected
build, or guide.

The durable report is:

- `reports/noelle-hexerei-local-stat-priority-diagnostic.json`
- 4,546,387 bytes
- SHA-256
  `08e39caf80afea209f563882ba29430bdb2229824addd5c9f9cb0013a0d420cc`

It is offline-only under `scripts/guide-factory`. No application, Worker,
production preset, or distributed bundle imports it.

## Authenticated boundary

The report authenticates an exact 116-path raw-byte and hash closure:

- checkpoint 55's complete 112-path input closure;
- the durable checkpoint-55 response-surface report;
- the reusable technical-point evaluator;
- the checkpoint-56 core;
- the checkpoint-56 CLI.

The closure contains 14 JSON files and the same two possible beta gzip inputs
already present in the inherited 80-path first-party runtime graph. Every
supplied byte sequence must match the current workspace file, and every
generated-from hash is recomputed from those bytes.

Checkpoint 55 is freshly authenticated from the bounded outer input. The new
evaluator then exactly reproduces all 24 source-aligned checkpoint-55 cells in
the default `all-theosis` and four-Curiosity-stack context. Exact reproduction
includes the artifact sheet, entered and effective talents, applicable-buff
trace as an order-independent canonical multiset, formula projection,
direct/compiled totals, and observed Noelle stats.

## Experimental lattice

Only the Sands listed by the authenticated source branch is admitted:

- lower investment: ATK% Sands;
- high investment: DEF% Sands.

The technical factors are:

- six entered investment witnesses;
- two unordered source-listed Circlets: CR and CD;
- Gest of the Mighty Wolf R1 and R5;
- Nicole `all-theosis` and `hexerei-theosis` modes;
- Husk four and zero Curiosity stacks;
- one baseline plus one independent current-average-roll neighbor for each of
  CR, CD, ATK%, and DEF%.

This produces:

- 96 baselines;
- 384 probe cells;
- 480 fresh objective `TeamBuild` cells;
- 480 fresh no-Husk control `TeamBuild` instances that verify the two-piece
  DEF contribution numerically;
- 480 scoped 5/5/3 Normal-prefix projections;
- 480 direct evaluations;
- 480 compiled evaluations;
- 480 direct/compiled agreements;
- zero ER constraints.

Every probe is an aggregate `StatSheet` neighbor, not a legal five-artifact
allocation. The report records all slots on which that substat would not
conflict with the fixed main stat, but selects no slot.

## Durable computation evidence

The first implementation embedded every full evaluation and produced a roughly
10 MB report. The final artifact deduplicates the inspectable evidence into:

- 20 exact normalized artifact sheets covering 480 references;
- 96 exact applicable-buff traces covering 480 references.

Every catalog payload is content-addressed. Every cell resolves exactly one
sheet and one trace, and the compact cell plus those payloads reconstructs the
full technical evaluator SHA-256 exactly.

Each fresh evaluation verifies:

- the entered source predicate and separate runtime-effective talent levels;
- Noelle Normal and Burst talent evidence;
- exact Gest R1/R5 source, target, class, and values;
- exact Geo resonance source, target, and values;
- exact Husk Curiosity source, target, and values;
- a controlled +30% DEF base-sheet differential from the configured Husk
  two-piece half-set;
- Nicole's base Kenosis in both modes;
- Nicole's Theosis uplift for Noelle only in `all-theosis` mode;
- the exact registered Nicole P1 target in both modes, including the retained
  `{ receiver: "team", factions: ["Hexerei"] }` target in Hexerei-only mode;
- no buffs from the placeholder teammate weapons;
- tagged Geo Normal damage-bonus observation;
- exact formula-index restoration;
- absence of an ER compiler constraint.

Husk zero means zero Curiosity stacks while 4pc Husk remains configured. The
authenticated runtime class still supplies its `def%-30` two-piece half-set; it
does not mean that the artifact set was removed.

## Local marginals and sensitivity

The report creates 384 parent-to-probe marginals. Each pair has the same
witness, source-aligned Sands, Circlet, refinement, Nicole mode, and Husk state,
and differs only by one current average roll.

It also creates 480 single-axis support-context edges:

- 240 Nicole-mode edges;
- 240 Husk-stack edges.

These edges record response sensitivity only. They contain no source ordering,
choice, winner, or recommendation.

## Source-order diagnostics

The source records three ordinal groups per investment branch. CR and CD share
one group and are never ordered against one another. Checkpoint 56 compares
only adjacent groups:

- lower: CR > ATK%, CD > ATK%, ATK% > DEF%;
- high: CR > DEF%, CD > DEF%, DEF% > ATK%.

Across 288 exact local contexts, the observed outcomes are:

- 242 `source-order-aligned`;
- 46 `source-order-counterexample`;
- 0 `within-tolerance-inconclusive`.

By relation:

| Source relation | Aligned | Counterexample |
| --- | ---: | ---: |
| Lower CR > ATK% | 32 | 0 |
| Lower CD > ATK% | 32 | 0 |
| Lower ATK% > DEF% | 16 | 16 |
| High CR > DEF% | 62 | 2 |
| High CD > DEF% | 52 | 12 |
| High DEF% > ATK% | 48 | 16 |

The 72 witness/Circlet/refinement summaries aggregate only the four tested
Nicole/Husk states:

- 58 aligned across the tested sensitivity grid;
- 10 counterexample across the tested sensitivity grid;
- 4 context-dependent or inconclusive.

The lower ATK%-over-DEF% split is witness-driven: C0/Q9 aligns while C5/Q9 is a
counterexample in every tested Circlet/refinement/Nicole/Husk context. That is
why the report retains witness-local evidence rather than averaging the lower
branch.

These outcomes test one static calculator fixture. Alignment does not validate
the source universally, and a counterexample does not prove that the source is
wrong. Both remain review targets for future factory policy.

## Operation and authority boundaries

The operation ledger explicitly scopes 480 objective builds, 480 half-set
control builds, and 480 direct/compiled evaluation pairs to checkpoint-56
local work and excludes upstream authentication operations. It separately
records that checkpoint 55 freshly recomputed 48 response cells.

Source-backed facts remain limited to the exact team, investment predicates,
branch-local Sands, unordered Circlets, ordinal substat groups, Husk 4pc
observation, Gest applicability, and Normal-prefix counts.

Refinement endpoints, support modes, Husk stacks, enemy/static fixture, and
one-roll probes remain Guide Factory-authored technical factors. Local
marginals and ordinal outcomes are computed validation observations.

The report produces zero:

- selected stats, Circlets, refinements, support options, Husk states, or
  artifact placements;
- scalar weights or branch averages;
- optimizer or AutoTune runs;
- legal or ideal stat allocations;
- source rotation replays or team-total computations;
- ER computations.

All guide, build, equipment, stat-priority, rank, winner, player-damage, DPS,
timing, ideal-allocation, and ER capabilities remain false.

## Verification

- focused checkpoint-56 tests: 15/15 passed;
- complete Guide Factory suite: 103 files passed, 1,026 tests passed, 2
  intentional skips;
- `validate.ts --defer-er`: 0 errors and the same 12 known legacy weapon-type
  warnings;
- Guide Factory TypeScript: passed;
- application and test TypeScript: passed;
- dependency boundary: 758 modules and 3,994 dependencies, no violations;
- production build: passed with the existing large-chunk advisory.

## Next non-ER checkpoint

The next step should exercise factory control flow rather than majority-vote
the 242/46 partition. A request-conditioned admission gate can resolve entered
investment facts to exactly one checkpoint-53 partial candidate and consume all
16 Circlet/refinement/Nicole/Husk diagnostics for each adjacent relation.

A relation should be technically admitted only when every tested context
aligns. Any counterexample or inconclusive result should leave it withheld with
explicit evidence IDs and blocking reasons. The candidate's still-null
Circlet, refinement, artifact placement, legal allocation, timing, and ER
fields must remain null.

ER work remains deferred.
