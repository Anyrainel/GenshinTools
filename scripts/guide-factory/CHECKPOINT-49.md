# Checkpoint 49: Xiao FFXX five-star source-group validation diagnostic

This checkpoint compares checkpoint 48's technically eligible observations
with the two authenticated five-star source rank groups. It exposes overlap and
every cross-group counterexample under one incomplete wrapper fixture. It does
not convert the source order or computed values into a weapon ranking.

## Exact authenticated boundary

The durable report authenticates 124 exact raw-byte inputs: checkpoint 48's
complete 121-path boundary, the durable checkpoint-48 report, and the
checkpoint-49 core and CLI. Fourteen JSON inputs require both raw-byte and
parsed-object parity.

Checkpoint 48 is fresh-authenticated before use, recursively rebuilding the
checkpoint-47 replay evidence and every earlier Xiao source/candidate boundary.
Checkpoint 49 makes zero additional calculator replay calls.

## Validation-only comparison policy

The compared metric is checkpoint 48's authenticated unit-expanded direct
total. Larger values are related using only a floating-point equality envelope:
an absolute tolerance of `1e-9` and a relative tolerance of `1e-12`. This is not
a gameplay-significance threshold.

The diagnostic enumerates the complete three-by-two cross-product between
source rank groups 1 and 2. It computes within-group extrema for the envelope,
but no group average, aggregate score, member-to-member relation, damage sort,
or factory rank. Pair serialization is technical-identity-based rather than
numeric.

## Observed validation targets

The two five-star group envelopes overlap under this fixture. Across the six
cross-group pairs, two are source-order-aligned, four are source-order
counterexamples, and none are tolerance-equivalent. Every relation is labelled
as a validation target only. A counterexample is not proof that the source is
wrong, that the calculator is correct, or that the computed order should
replace the source order.

Tied membership within each source group remains an ordinal source fact; it is
not interpreted as exact equal damage. No within-group rank is derived.

## Deathmatch and capability boundary

Deathmatch remains an authenticated excluded observation because there is no
authenticated cross-rarity order. It participates in zero pairs and receives
no relative position or factory rank.

The report performs a validation-only pairwise diagnostic, but supports no
player-facing damage comparison, guide, team, equipment, build, stat, winner,
selection, source correction, complete-build, Circlet, substat, ideal-roll,
rotation, gameplay, DPS, generator, optimizer, AutoTune, or ER claim.

## Next non-ER boundary

The next experiment may introduce guarded Circlet and substat variants, but it
must preserve the incomplete-fixture warnings and validate every added choice
against authenticated source targets. The four current counterexamples should
remain visible test cases rather than being optimized away or promoted as
recommendations. ER remains deferred.

## Durable output and verification

The checked-in report is 45,453 bytes with SHA-256
`2ce22c321d9194b4c6875e449918832e90e208b48403b5e5764fa0eab4a744d4`.

Focused checkpoint tests pass 11/11. The complete Guide Factory suite passes
96 files with 923 tests passed and two historical tests skipped. The global
validator finishes with zero errors and twelve pre-existing catalog warnings
when Diona comparison and ER calibration are explicitly deferred. Guide
Factory TypeScript, the application-wide type check, and dependency-boundary
validation also pass.
